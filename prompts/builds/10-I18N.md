# Track 10 — Internationalization (I18N)

> Translate documentation and user-facing strings to make XActions accessible to non-English developers worldwide. Competing projects like twikit (4k★, Japan-based) and bisguzar/twitter-scraper (Go, global community) prove that multi-language support directly correlates with international adoption. This track adds translations for the top 5 non-English developer communities: Japanese (ja), Chinese Simplified (zh), Spanish (es), Korean (ko), and Portuguese (pt-BR).

---

## Research Before Starting

```
README.md                            — Main English README (structure, sections)
docs/                                — All English documentation files
dashboard/                           — HTML pages (user-facing text)
src/cli/index.js                     — CLI user-facing strings (help, messages)
src/mcp/server.js                    — MCP tool descriptions
package.json                         — Package description
CONTRIBUTING.md                      — Contributing guidelines
skills/                              — 31 skill files with descriptions
```

Study competitor i18n implementations:
- `d60/twikit` — README available in English + Japanese
- `nicepkg/gpt-runner` — README_CN.md pattern for Chinese
- `discordjs/discord.js` — Uses separate locale folders

### Approach

1. **Documentation-first** — Translate README, quickstart, and key docs
2. **CLI messages** — Extract strings to locale files for CLI output
3. **Dashboard** — Add language switcher with translated UI strings
4. **Structured layout** — `i18n/` directory with per-language folders

---

## Architecture

```
i18n/
  README.md              ← i18n contributing guide (how to add a new language)
  languages.json         ← Registry of supported languages and completion %
  ja/                    ← Japanese
    README.md
    CONTRIBUTING.md
    docs/
      getting-started.md
      cli-reference.md
      api-reference.md
      python-sdk.md
    cli/
      messages.json
    dashboard/
      strings.json
  zh/                    ← Chinese Simplified
    README.md
    CONTRIBUTING.md
    docs/
      getting-started.md
      cli-reference.md
      api-reference.md
    cli/
      messages.json
    dashboard/
      strings.json
  es/                    ← Spanish
    README.md
    docs/
      getting-started.md
    cli/
      messages.json
  ko/                    ← Korean
    README.md
    docs/
      getting-started.md
    cli/
      messages.json
  pt-BR/                 ← Portuguese (Brazil)
    README.md
    docs/
      getting-started.md
    cli/
      messages.json
src/
  i18n/
    index.js             ← i18n runtime module
    locales/
      en.json            ← English strings (source of truth)
      ja.json
      zh.json
      es.json
      ko.json
      pt-BR.json
dashboard/
  js/
    i18n.js              ← Browser-side i18n loader
```

---

## Prompts

### Prompt 1: I18N Infrastructure Setup

```
Create the i18n infrastructure.

Create i18n/README.md:
# XActions Internationalization

This directory contains translations for XActions documentation and UI.

## Supported Languages

| Language | Code | README | Docs | CLI | Dashboard | Completion |
|---|---|---|---|---|---|---|
| English | en | ✅ | ✅ | ✅ | ✅ | 100% |
| Japanese | ja | ✅ | ✅ | ✅ | ✅ | 100% |
| Chinese (Simplified) | zh | ✅ | ✅ | ✅ | ✅ | 100% |
| Spanish | es | ✅ | 🔄 | ✅ | ❌ | 60% |
| Korean | ko | ✅ | 🔄 | ✅ | ❌ | 60% |
| Portuguese (Brazil) | pt-BR | ✅ | 🔄 | ✅ | ❌ | 60% |

## How to Add a New Language

1. Create a folder: `i18n/<language-code>/`
2. Copy `i18n/ja/README.md` as a template
3. Translate the content (do NOT translate code blocks, URLs, or technical terms)
4. Add CLI strings: copy `src/i18n/locales/en.json` → `src/i18n/locales/<code>.json`
5. Register in `i18n/languages.json`
6. Submit a PR with the language code in the title: `i18n(ko): Korean README`

## Translation Guidelines

- Do NOT translate:
  - Code blocks and examples
  - Package names (xactions, npm, pip)
  - CLI commands (xactions login, xactions search)
  - Technical terms when no standard translation exists
  - URLs and links (including relative paths)
- DO translate:
  - Prose descriptions and explanations
  - Section headers
  - Feature descriptions
  - Error messages
  - Comments within code examples (if helpful)

Create i18n/languages.json:
{
  "languages": [
    { "code": "en", "name": "English", "nativeName": "English", "completion": 100 },
    { "code": "ja", "name": "Japanese", "nativeName": "日本語", "completion": 100 },
    { "code": "zh", "name": "Chinese (Simplified)", "nativeName": "简体中文", "completion": 100 },
    { "code": "es", "name": "Spanish", "nativeName": "Español", "completion": 60 },
    { "code": "ko", "name": "Korean", "nativeName": "한국어", "completion": 60 },
    { "code": "pt-BR", "name": "Portuguese (Brazil)", "nativeName": "Português (Brasil)", "completion": 60 }
  ],
  "defaultLanguage": "en",
  "translationKeys": {
    "readme": true,
    "contributing": true,
    "gettingStarted": true,
    "cliReference": true,
    "apiReference": true,
    "cliMessages": true,
    "dashboardStrings": true
  }
}
```

### Prompt 2: English CLI String Extraction

```
Extract all user-facing strings from src/cli/index.js into a structured JSON locale file.

Create src/i18n/index.js:

import { readFileSync } from 'fs';
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';

const __dirname = dirname(fileURLToPath(import.meta.url));

let currentLocale = 'en';
let strings = {};

export function setLocale(locale) {
  currentLocale = locale;
  const filePath = join(__dirname, 'locales', `${locale}.json`);
  try {
    strings = JSON.parse(readFileSync(filePath, 'utf-8'));
  } catch {
    // Fallback to English
    const enPath = join(__dirname, 'locales', 'en.json');
    strings = JSON.parse(readFileSync(enPath, 'utf-8'));
  }
}

export function t(key, params = {}) {
  let str = strings[key] || key;
  for (const [k, v] of Object.entries(params)) {
    str = str.replace(`{${k}}`, v);
  }
  return str;
}

export function getLocale() {
  return currentLocale;
}

export function getSupportedLocales() {
  return ['en', 'ja', 'zh', 'es', 'ko', 'pt-BR'];
}

// Auto-initialize with English
setLocale('en');

Create src/i18n/locales/en.json:
{
  "cli.login.success": "✅ Logged in as @{username}",
  "cli.login.failed": "❌ Login failed: {error}",
  "cli.login.prompt.username": "Twitter username:",
  "cli.login.prompt.password": "Password:",
  "cli.login.prompt.email": "Email (for verification):",
  
  "cli.profile.header": "👤 {name} (@{username})",
  "cli.profile.bio": "📝 {bio}",
  "cli.profile.stats": "📊 {followers} followers · {following} following · {tweets} tweets",
  "cli.profile.notFound": "❌ User @{username} not found",

  "cli.search.header": "🔍 Searching for: {query}",
  "cli.search.result": "@{username}: {text}",
  "cli.search.stats": "❤️ {likes} · 🔁 {retweets} · 💬 {replies}",
  "cli.search.noResults": "No results found for \"{query}\"",

  "cli.tweet.posted": "✅ Tweet posted: https://x.com/i/status/{id}",
  "cli.tweet.deleted": "✅ Tweet {id} deleted",
  "cli.tweet.notFound": "❌ Tweet not found",

  "cli.followers.header": "👥 Followers of @{username}:",
  "cli.followers.item": "@{username} — {followers} followers",
  "cli.followers.count": "Total: {count} followers",

  "cli.unfollow.progress": "Unfollowing {current}/{total}...",
  "cli.unfollow.success": "✅ Unfollowed @{username}",
  "cli.unfollow.complete": "🎉 Unfollowed {count} users",

  "cli.error.notLoggedIn": "❌ Not logged in. Run: xactions login",
  "cli.error.rateLimited": "⏳ Rate limited. Retrying in {seconds}s...",
  "cli.error.network": "🌐 Network error: {message}",
  "cli.error.unknown": "❌ Error: {message}",

  "cli.common.loading": "Loading...",
  "cli.common.done": "Done!",
  "cli.common.cancelled": "Cancelled"
}

Go through every console.log, ora spinner text, and chalk output in src/cli/index.js and extract strings.
At least 60 keys covering all CLI commands. Use dot-notation namespacing.
```

### Prompt 3: Japanese README Translation

```
Create i18n/ja/README.md — full Japanese translation of the root README.md.

Translate the complete README keeping all:
- Code blocks untouched (exact same code)
- Links preserved (update relative paths if needed)
- Badge URLs unchanged
- Command-line examples unchanged
- Package names (xactions, npm, puppeteer) untranslated

At the very top of the file:
# XActions

[English](../../README.md) | **日本語** | [中文](../zh/README.md) | [Español](../es/README.md) | [한국어](../ko/README.md) | [Português](../pt-BR/README.md)

> ⚡ 完全なX/Twitter自動化ツールキット — スクレイパー、AIエージェント用MCPサーバー（Claude/GPT）、CLI、ブラウザスクリプト。API料金不要。オープンソース。

Translation guidelines:
- Technical terms: 
  - "scraper" → "スクレイパー"
  - "browser scripts" → "ブラウザスクリプト"  
  - "CLI" → "CLI" (keep as-is)
  - "MCP server" → "MCPサーバー"
  - "rate limiting" → "レート制限"
  - "authentication" → "認証"
  - "cookie" → "Cookie" (keep as-is)
  - "API" → "API" (keep as-is)
  - "dashboard" → "ダッシュボード"
- Style: Formal (ですます調) for documentation
- Include feature list, installation, usage, CLI reference, API reference outlines
- All sections from the English README must be present
```

### Prompt 4: Japanese CLI Messages

```
Create src/i18n/locales/ja.json — Japanese translation of all CLI strings.

Translate every key from en.json:
{
  "cli.login.success": "✅ @{username} としてログインしました",
  "cli.login.failed": "❌ ログイン失敗: {error}",
  "cli.login.prompt.username": "Twitterユーザー名:",
  "cli.login.prompt.password": "パスワード:",
  "cli.login.prompt.email": "メールアドレス（認証用）:",
  
  "cli.profile.header": "👤 {name} (@{username})",
  "cli.profile.bio": "📝 {bio}",
  "cli.profile.stats": "📊 フォロワー {followers}人 · フォロー {following}人 · ツイート {tweets}件",
  "cli.profile.notFound": "❌ ユーザー @{username} が見つかりません",

  "cli.search.header": "🔍 検索中: {query}",
  "cli.search.noResults": "「{query}」の検索結果はありません",

  "cli.tweet.posted": "✅ ツイートを投稿しました: https://x.com/i/status/{id}",
  "cli.tweet.deleted": "✅ ツイート {id} を削除しました",

  "cli.followers.header": "👥 @{username} のフォロワー:",
  "cli.followers.count": "合計: {count} フォロワー",

  "cli.unfollow.progress": "フォロー解除中 {current}/{total}...",
  "cli.unfollow.success": "✅ @{username} のフォローを解除しました",
  "cli.unfollow.complete": "🎉 {count}人のフォローを解除しました",

  "cli.error.notLoggedIn": "❌ ログインしていません。実行: xactions login",
  "cli.error.rateLimited": "⏳ レート制限中。{seconds}秒後に再試行します...",
  "cli.error.network": "🌐 ネットワークエラー: {message}",
  "cli.error.unknown": "❌ エラー: {message}",

  "cli.common.loading": "読み込み中...",
  "cli.common.done": "完了！",
  "cli.common.cancelled": "キャンセルされました"
}

All 60+ keys translated. Emojis preserved. Placeholders {variable} unchanged.
Natural Japanese phrasing, not machine-translated artifacts.
```

### Prompt 5: Japanese Documentation

```
Create translated documentation for Japanese:

i18n/ja/docs/getting-started.md:
- Full translation of the quickstart guide
- Installation, first run, cookie setup, first scrape
- All code blocks in English (unchanged)
- Comments in code examples translated to Japanese

i18n/ja/docs/cli-reference.md:
- CLI command reference
- All commands, options, examples
- Output descriptions in Japanese

i18n/ja/docs/api-reference.md:
- Node.js library API reference
- Class and method descriptions in Japanese
- Parameter types and return types (English/technical)
- Example code with Japanese comments

i18n/ja/CONTRIBUTING.md:
- Translation of CONTRIBUTING.md
- How to contribute, code style, PR process

All files include navigation header:
[English](../../../docs/...) | **日本語** | [中文](../zh/docs/...) ...

Use ですます調 (polite form) throughout.
```

### Prompt 6: Chinese Simplified README and Docs

```
Create i18n/zh/README.md — Chinese Simplified translation of the root README.md.

All the same structural rules as Japanese:
- Language switcher at top
- All code blocks unchanged
- Technical terms:
  - "scraper" → "爬虫" or "抓取工具"
  - "browser scripts" → "浏览器脚本"
  - "rate limiting" → "速率限制"
  - "authentication" → "认证" or "身份验证"
  - "cookie" → "Cookie" (keep as-is)
  - "dashboard" → "仪表板" or "控制面板"

> ⚡ 完整的X/Twitter自动化工具包 — 爬虫、AI代理MCP服务器（Claude/GPT）、CLI、浏览器脚本。无需API费用。开源。

Create i18n/zh/docs/getting-started.md:
- Full quickstart guide in Simplified Chinese

Create src/i18n/locales/zh.json:
{
  "cli.login.success": "✅ 已以 @{username} 身份登录",
  "cli.login.failed": "❌ 登录失败: {error}",
  "cli.profile.stats": "📊 {followers} 粉丝 · {following} 关注 · {tweets} 推文",
  "cli.search.header": "🔍 搜索: {query}",
  "cli.error.notLoggedIn": "❌ 未登录。请运行: xactions login",
  "cli.error.rateLimited": "⏳ 触发速率限制。{seconds}秒后重试...",
  "cli.common.loading": "加载中...",
  "cli.common.done": "完成！",
  ...all 60+ keys
}

i18n/zh/CONTRIBUTING.md: translated contributing guide.
```

### Prompt 7: Spanish README and CLI Messages

```
Create i18n/es/README.md — Spanish translation.

> ⚡ El kit de herramientas completo para automatización de X/Twitter — Scrapers, servidor MCP para agentes de IA (Claude/GPT), CLI, scripts de navegador. Sin costes de API. Código abierto.

Technical terms:
- "scraper" → "scraper" (keep, widely used in Spanish dev community)
- "browser scripts" → "scripts de navegador"
- "rate limiting" → "limitación de tasa" or "límite de solicitudes"
- "authentication" → "autenticación"
- "dashboard" → "panel de control"
- "followers" → "seguidores"
- "following" → "siguiendo"

Create src/i18n/locales/es.json:
{
  "cli.login.success": "✅ Sesión iniciada como @{username}",
  "cli.login.failed": "❌ Error de inicio de sesión: {error}",
  "cli.profile.stats": "📊 {followers} seguidores · {following} siguiendo · {tweets} tweets",
  "cli.search.header": "🔍 Buscando: {query}",
  "cli.error.notLoggedIn": "❌ No has iniciado sesión. Ejecuta: xactions login",
  "cli.error.rateLimited": "⏳ Límite de tasa alcanzado. Reintentando en {seconds}s...",
  "cli.common.loading": "Cargando...",
  "cli.common.done": "¡Hecho!",
  ...all 60+ keys
}

i18n/es/docs/getting-started.md: Spanish quickstart guide.
Use neutral Latin American Spanish (not region-specific).
```

### Prompt 8: Korean README and CLI Messages

```
Create i18n/ko/README.md — Korean translation.

> ⚡ 완벽한 X/Twitter 자동화 도구 — 스크래퍼, AI 에이전트용 MCP 서버(Claude/GPT), CLI, 브라우저 스크립트. API 비용 없음. 오픈소스.

Technical terms:
- "scraper" → "스크래퍼"
- "browser scripts" → "브라우저 스크립트"
- "rate limiting" → "속도 제한"
- "authentication" → "인증"
- "dashboard" → "대시보드"
- "followers" → "팔로워"
- "following" → "팔로잉"

Create src/i18n/locales/ko.json:
{
  "cli.login.success": "✅ @{username}(으)로 로그인했습니다",
  "cli.login.failed": "❌ 로그인 실패: {error}",
  "cli.profile.stats": "📊 팔로워 {followers}명 · 팔로잉 {following}명 · 트윗 {tweets}개",
  "cli.search.header": "🔍 검색 중: {query}",
  "cli.error.notLoggedIn": "❌ 로그인되지 않았습니다. 실행: xactions login",
  "cli.error.rateLimited": "⏳ 속도 제한됨. {seconds}초 후 재시도...",
  "cli.common.loading": "로딩 중...",
  "cli.common.done": "완료!",
  ...all 60+ keys
}

i18n/ko/docs/getting-started.md: Korean quickstart guide.
Use 해요체 (polite informal) style for documentation.
```

### Prompt 9: Portuguese (Brazil) README and CLI Messages

```
Create i18n/pt-BR/README.md — Brazilian Portuguese translation.

> ⚡ O kit completo de automação X/Twitter — Scrapers, servidor MCP para agentes de IA (Claude/GPT), CLI, scripts de navegador. Sem taxas de API. Código aberto.

Technical terms:
- "scraper" → "scraper" (keep, widely used in Brazilian dev community)
- "browser scripts" → "scripts de navegador"
- "rate limiting" → "limitação de taxa"
- "authentication" → "autenticação"
- "dashboard" → "painel de controle"
- "followers" → "seguidores"
- "following" → "seguindo"

Create src/i18n/locales/pt-BR.json:
{
  "cli.login.success": "✅ Logado como @{username}",
  "cli.login.failed": "❌ Falha no login: {error}",
  "cli.profile.stats": "📊 {followers} seguidores · {following} seguindo · {tweets} tweets",
  "cli.search.header": "🔍 Pesquisando: {query}",
  "cli.error.notLoggedIn": "❌ Não conectado. Execute: xactions login",
  "cli.error.rateLimited": "⏳ Limite de taxa atingido. Tentando novamente em {seconds}s...",
  "cli.common.loading": "Carregando...",
  "cli.common.done": "Pronto!",
  ...all 60+ keys
}

i18n/pt-BR/docs/getting-started.md: Portuguese quickstart guide.
Use Brazilian Portuguese conventions and vocabulary.
```

### Prompt 10: Dashboard I18N Support

```
Create browser-side i18n support for the dashboard.

Create dashboard/js/i18n.js:

const DASHBOARD_STRINGS = {
  en: {
    "nav.home": "Home",
    "nav.analytics": "Analytics",
    "nav.automations": "Automations",
    "nav.settings": "Settings",
    "nav.docs": "Docs",
    "header.title": "XActions Dashboard",
    "header.subtitle": "Twitter/X Automation Control Panel",
    "login.title": "Login to XActions",
    "login.username": "Username",
    "login.password": "Password",
    "login.submit": "Login",
    "dashboard.followers": "Followers",
    "dashboard.following": "Following",
    "dashboard.tweets": "Tweets",
    "dashboard.engagement": "Engagement Rate",
    "automation.start": "Start",
    "automation.stop": "Stop",
    "automation.running": "Running",
    "automation.stopped": "Stopped",
    "error.network": "Network error",
    "error.auth": "Authentication required",
    // ... 40+ keys covering all dashboard pages
  },
  ja: { ... },
  zh: { ... },
};

class DashboardI18n {
  constructor() {
    this.locale = this.detectLocale();
  }

  detectLocale() {
    const saved = localStorage.getItem('xactions-lang');
    if (saved && DASHBOARD_STRINGS[saved]) return saved;
    const browserLang = navigator.language.split('-')[0];
    return DASHBOARD_STRINGS[browserLang] ? browserLang : 'en';
  }

  t(key) {
    return DASHBOARD_STRINGS[this.locale]?.[key] 
        || DASHBOARD_STRINGS.en[key] 
        || key;
  }

  setLocale(locale) {
    this.locale = locale;
    localStorage.setItem('xactions-lang', locale);
    this.applyToDOM();
  }

  applyToDOM() {
    document.querySelectorAll('[data-i18n]').forEach(el => {
      const key = el.getAttribute('data-i18n');
      el.textContent = this.t(key);
    });
    document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
      const key = el.getAttribute('data-i18n-placeholder');
      el.placeholder = this.t(key);
    });
  }
}

const i18n = new DashboardI18n();
document.addEventListener('DOMContentLoaded', () => i18n.applyToDOM());

Create a language switcher component that can be dropped into any dashboard page.
```

### Prompt 11: CLI I18N Integration

```
Update src/cli/index.js to use the i18n system.

Import and use the i18n module:

import { t, setLocale, getSupportedLocales } from '../i18n/index.js';

// Add language option to the root program:
program.option('--lang <locale>', 'Set language (en, ja, zh, es, ko, pt-BR)', 'en');

// Before executing any command:
program.hook('preAction', (thisCommand) => {
  const opts = thisCommand.optsWithGlobals();
  if (opts.lang) {
    setLocale(opts.lang);
  }
});

// Add a language command:
program
  .command('lang [locale]')
  .description('Set or show CLI language')
  .action((locale) => {
    if (!locale) {
      console.log(`Current: ${getLocale()}`);
      console.log(`Available: ${getSupportedLocales().join(', ')}`);
      return;
    }
    setLocale(locale);
    console.log(t('cli.lang.set', { locale }));
  });

// Replace all hardcoded strings with t() calls:
// BEFORE:
//   console.log(`✅ Logged in as @${username}`);
// AFTER:
//   console.log(t('cli.login.success', { username }));

// BEFORE:
//   console.log(`❌ Login failed: ${error.message}`);
// AFTER:
//   console.log(t('cli.login.failed', { error: error.message }));

Replace every user-facing hardcoded string in src/cli/index.js with t() calls.
Also support XACTIONS_LANG environment variable for locale override:
  const envLang = process.env.XACTIONS_LANG;
  if (envLang) setLocale(envLang);

Test: XACTIONS_LANG=ja xactions --help should show Japanese help text.
```

### Prompt 12: Root README Language Links

```
Update README.md to include language switcher at the top.

Add after the title/badges section:

**🌐 Language / 言語 / 语言:**
[English](README.md) | [日本語](i18n/ja/README.md) | [简体中文](i18n/zh/README.md) | [Español](i18n/es/README.md) | [한국어](i18n/ko/README.md) | [Português](i18n/pt-BR/README.md)

Also update the Table of Contents to include an "Internationalization" section.

Add a new section near the bottom:
## 🌐 Internationalization

XActions is available in multiple languages:

| Language | README | Docs | CLI | Dashboard |
|---|---|---|---|---|
| English 🇺🇸 | ✅ | ✅ | ✅ | ✅ |
| Japanese 🇯🇵 | ✅ | ✅ | ✅ | ✅ |
| Chinese 🇨🇳 | ✅ | ✅ | ✅ | ✅ |
| Spanish 🇪🇸 | ✅ | 🔄 | ✅ | ❌ |
| Korean 🇰🇷 | ✅ | 🔄 | ✅ | ❌ |
| Portuguese 🇧🇷 | ✅ | 🔄 | ✅ | ❌ |

Set CLI language:
\```bash
# Option 1: CLI flag
xactions --lang ja profile elonmusk

# Option 2: Environment variable
export XACTIONS_LANG=ja
xactions profile elonmusk
\```

Want to help translate? See [i18n/README.md](i18n/README.md) for guidelines.

NO other changes to README.md.
```

### Prompt 13: MCP Tool Descriptions I18N

```
Add internationalization support for MCP server tool descriptions.

The MCP server (src/mcp/server.js) exposes 140+ tools, each with a name and description.
Users running Claude Desktop in non-English locales benefit from translated descriptions.

Create src/i18n/mcp-descriptions.json:
{
  "en": {
    "x_login": "Login to Twitter/X with username and password",
    "x_get_profile": "Get a Twitter/X user profile by username",
    "x_search_tweets": "Search tweets by keyword or hashtag",
    "x_post_tweet": "Post a new tweet",
    "x_get_followers": "Get followers for a user",
    ...all 140+ tool descriptions
  },
  "ja": {
    "x_login": "ユーザー名とパスワードでTwitter/Xにログイン",
    "x_get_profile": "ユーザー名でTwitter/Xのプロフィールを取得",
    "x_search_tweets": "キーワードまたはハッシュタグでツイートを検索",
    ...
  },
  "zh": {
    "x_login": "使用用户名和密码登录Twitter/X",
    "x_get_profile": "通过用户名获取Twitter/X用户资料",
    ...
  }
}

Update src/mcp/server.js to optionally load translated descriptions:
- Check XACTIONS_LANG environment variable
- Default to English
- Fall back to English for any missing key

This does NOT change the tool names (always English). Only descriptions are translated.
```

### Prompt 14: I18N Tests

```
Create tests/i18n.test.js with vitest.

Tests:

1. test_en_locale_loads
   - setLocale('en') works
   - All keys return non-empty strings
   - No {placeholder} left unreplaced when params provided

2. test_ja_locale_loads
   - setLocale('ja') works
   - All keys from en.json also exist in ja.json

3. test_zh_locale_loads
   - Same coverage check for Chinese

4. test_fallback_to_english
   - setLocale('xx') falls back to English
   - Missing key returns key itself

5. test_t_function_replacement
   - t('cli.login.success', { username: 'test' }) returns correct string
   - Multiple {replacements} work
   - Missing params left as-is

6. test_all_locales_have_same_keys
   - Every locale file has the same set of keys as en.json
   - Flag any missing keys

7. test_no_html_in_translations
   - No locale string contains HTML tags (security)

8. test_placeholder_consistency
   - Same {placeholders} appear in all translations for each key

9. test_language_registry
   - languages.json parses correctly
   - All listed languages have locale files

10. test_cli_lang_flag
    - CLI accepts --lang flag without error

Also create a script to validate translation completeness:
scripts/validate-i18n.js:
- Reads en.json
- Checks each locale file for missing/extra keys
- Reports completion percentage
- Exit code 1 if any locale is below 90%
```

### Prompt 15: I18N GitHub Actions Workflow

```
Create .github/workflows/i18n-check.yml:

name: I18N Validation
on:
  push:
    paths:
      - 'i18n/**'
      - 'src/i18n/**'
  pull_request:
    paths:
      - 'i18n/**'
      - 'src/i18n/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - name: Validate translations
        run: node scripts/validate-i18n.js
      - name: Check for broken links in translated docs
        run: |
          for lang in ja zh es ko pt-BR; do
            echo "Checking i18n/$lang/..."
            find "i18n/$lang" -name '*.md' -exec grep -l '\[.*\](.*\.md)' {} \; | while read f; do
              grep -oP '\]\(\K[^)]+\.md' "$f" | while read link; do
                dir=$(dirname "$f")
                if [[ "$link" != http* ]] && [[ ! -f "$dir/$link" ]] && [[ ! -f "$link" ]]; then
                  echo "❌ Broken link in $f: $link"
                  exit 1
                fi
              done
            done
          done
      - name: Run i18n tests
        run: npx vitest run tests/i18n.test.js

Document in CONTRIBUTING.md:
- How translators can contribute
- Required keys and format
- CI will validate on PR
- Link to i18n/README.md
```

---

## Validation

After all 15 prompts are complete, verify:

```bash
# i18n infrastructure exists
ls -la i18n/
ls -la i18n/ja/ i18n/zh/ i18n/es/ i18n/ko/ i18n/pt-BR/
ls -la src/i18n/locales/

# All locale files parse as valid JSON
for f in src/i18n/locales/*.json; do
  python3 -c "import json; json.load(open('$f')); print(f'✅ {\"$f\"} is valid JSON')"
done

# Key count matches across locales
EN_KEYS=$(python3 -c "import json; print(len(json.load(open('src/i18n/locales/en.json'))))")
for lang in ja zh es ko pt-BR; do
  LANG_KEYS=$(python3 -c "import json; print(len(json.load(open('src/i18n/locales/$lang.json'))))")
  echo "$lang: $LANG_KEYS/$EN_KEYS keys"
done

# i18n module loads
node -e "import { t, setLocale } from './src/i18n/index.js'; setLocale('ja'); console.log(t('cli.login.success', { username: 'test' }));"

# Japanese README exists
cat i18n/ja/README.md | head -5

# Dashboard i18n loads
cat dashboard/js/i18n.js | head -5

# Validation script runs clean
node scripts/validate-i18n.js

# Tests pass
npx vitest run tests/i18n.test.js
```
